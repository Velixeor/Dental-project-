databaseChangeLog:
  - changeSet:
      id: 1
      author: postgres
      changes:
        - sql:
            sql: |
              CREATE TABLE project_designer.coupling (
                  id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,
                  project_id INT REFERENCES project_designer.project(id) ,
                  number_tooth1 INT NOT NULL,
                  number_tooth2 INT NOT NULL
              );
