databaseChangeLog:
  - changeSet:
      id: 1
      author: postgres
      changes:
        - sql:
            sql: |
              CREATE TABLE project_designer.service (
                  id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,
                  type_service_id INT REFERENCES project_designer.type_service(id),
                  service_price DECIMAL(10,2) NOT NULL CHECK (service_price >= 0),
                  material_id INT REFERENCES project_designer.material(id)
              );
