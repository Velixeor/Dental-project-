databaseChangeLog:
  - changeSet:
      id: 1
      author: postgres
      changes:
        - sql:
            sql: |
              CREATE TABLE project_designer.customer (
                  id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,
                  name VARCHAR(255) NOT NULL,
                  phone VARCHAR(255) ,
                  address VARCHAR(255),
                  company_id INT
              );
